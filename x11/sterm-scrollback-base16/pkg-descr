st is a minimalistic X terminal.

It consists of a single binary, configuration is done at compile-time by a
config.h file. Patched for scrollback and Base16 colour theme support.

WWW: http://st.suckless.org/
